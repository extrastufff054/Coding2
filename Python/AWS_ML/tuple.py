#A tuple is similar to a list, but you use them with parentheses instead of square brackets.
#The main difference is that a tuple is immutable, which means you can't change it once you create it.(list is mutable)

my_tuple = (1,2,3,4,5)
my_tuple[1:4]

print(my_tuple[1:4])